<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.splashandviewbager">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SocketCreate"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SplashAndViewBager">
        <activity android:name="com.example.splashandviewbager.screens.chatScreens.ChatGroup"></activity>
        <activity android:name="com.example.splashandviewbager.screens.HomeScreens.SearshUser"></activity>
        <activity android:name="com.example.splashandviewbager.screens.HomeScreens.UserScreen"></activity>
        <activity android:name="com.example.splashandviewbager.screens.HomeScreens.GroupsScreen"></activity>
        <activity
            android:name="com.example.splashandviewbager.screens.chatScreens.ChatScreen"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>