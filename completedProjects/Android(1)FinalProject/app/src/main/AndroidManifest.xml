<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finaldemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalDemo">
        <activity android:name=".SearshScreen"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".screens.ProductDetails" />
        <activity android:name=".screens.AddProduct" />
        <activity android:name=".screens.signingscreens.SignUpScreen" />
        <activity
            android:theme="@style/splashTheme"
            android:name=".screens.signingscreens.SignInScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>